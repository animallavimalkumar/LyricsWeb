# Environment Variables for Lyrics Web App
# Copy this file to .env and fill in the values

# Server port for local development
PORT=5000

# Node environment (development/production)
NODE_ENV=development

# API URL - leave empty for relative paths (recommended for Vercel deployment)
# For local development with separate backend, set to http://localhost:5000
VITE_API_URL=
